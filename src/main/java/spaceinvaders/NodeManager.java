package spaceinvaders;

import javafx.scene.Node;

/**
 * Classes inherited from this interface configure the node inside and have a function that returns the node
 */
public interface NodeManager {

    Node getNode();
}